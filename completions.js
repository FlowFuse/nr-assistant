module.exports = (RED) => {
    // Dummy module.
    // This causes NR to load the associated html part of the plugin where the client side
    // code for completions are defined.
    // The actual completions logic is in the lib/assistant.js file.
}
